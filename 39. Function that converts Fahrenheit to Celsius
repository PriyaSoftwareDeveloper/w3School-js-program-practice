<!DOCTYPE html>
<html>
  <head>
    <title>Function that converts Fahrenheit to Celsius</title>
  </head>
  <body>
    <h2>Function that converts Fahrenheit to Celsius</h2>

    <p>Invoke (call) a function that converts from Fahrenheit to Celsius:</p>

    <p id="demo"></p>
  </body>
  <script>
    function toCelsius(f) {
      return (5 / 9) * (f - 32);
    }

    let value = toCelsius(77);
    document.getElementById("demo").innerHTML = value;
  </script>
</html>
